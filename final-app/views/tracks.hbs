<h2> {{name}} </h2>
{{#each tracks}}
<div class="media">
	<div class="media-left">
		<a href="#">
			<img class="media-object" src="{{imageURL}}" style="width:110px; height:110px;">
		</a>
	</div>
	<div class="media-body">
		<h4 class="media-heading">{{name}}</h4>
		<audio controls>
			<source src="{{preview}}" type="audio/ogg">
		</audio>

	</div>
</div>
{{/each}}
